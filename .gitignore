# Data files (large binaries)
*.duckdb
*.dbn
*.parquet
*.csv
*.xlsx
# Forward-test workflow assets are committed, not data
!Fractal Sweep/forward_test/trade_log_template.csv
!Fractal Sweep/forward_test/forward_test_tracker.xlsx

# Logs
*.log

# macOS
.DS_Store

# Python
__pycache__/
*.pyc
*.pyo
.venv/

# Secrets
.env
.claude/settings.json
.claude/settings.local.json
.claude-design/

# Generated stats
# Sweep+CISD dashboard stats (~128 MB) exceed GitHub's push limit.
# Treat as a build artifact — regenerate locally via
# `python3 Fractal\ Sweep/model_stats.py`.
Fractal Sweep/model_stats.json

# Analysis engine derived data (parquet files, gitignored build artifact)
Analysis/data/
